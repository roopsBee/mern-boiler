import React from "react";
import { Typography } from "@material-ui/core";

const Home = (props) => {
  return (
    <div>
      <Typography color="primary" variant="h1">
        Home
      </Typography>
    </div>
  );
};

export default Home;
